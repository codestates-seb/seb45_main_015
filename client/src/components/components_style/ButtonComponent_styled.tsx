import { styled } from "styled-components";
import globalTokens from "../../design_tokens/global.json";

// ---------------Large
export const LargeButtonTypeA = styled.button`
  width: 100%;
  height: 100%;
  background-color: ${globalTokens.Button.default.value};
  color: #ffffff;
  font-size: 21px;
  cursor: pointer;
  border-radius: 20px;
  border: none;

  &:hover {
    background-color: #ffffff;
    color: ${globalTokens.Button.default.value};
    border: 2px solid ${globalTokens.Button.default.value};
  }

  @media (max-width: 768px) {
  }
`;
export const LargeButtonTypeB = styled.button`
  width: 100%;
  height: 100%;
  background-color: ${globalTokens.Button.default.value};
  color: #ffffff;
  font-size: 21px;
  cursor: pointer;
  border-radius: 90px;
  border: none;

  &:hover {
    background-color: #ffffff;
    color: ${globalTokens.Button.default.value};
    border: 2px solid ${globalTokens.Button.default.value};
  }

  @media (max-width: 768px) {
  }
`;
export const LargeButtonTypeC = styled.button`
  width: 100%;
  height: 100%;
  background-color: #ffffff;
  color: ${globalTokens.Button.default.value};
  font-size: 21px;
  cursor: pointer;
  border-radius: 20px;
  border: 2px solid ${globalTokens.Button.default.value};

  &:hover {
    background-color: ${globalTokens.Button.default.value};
    color: #ffffff;
    border: 2px solid ${globalTokens.Button.default.value};
  }

  @media (max-width: 768px) {
  }
`;
export const LargeButtonTypeD = styled.button`
  width: 100%;
  height: 100%;
  background-color: #ffffff;
  color: ${globalTokens.Button.default.value};
  font-size: 21px;
  cursor: pointer;
  border-radius: 90px;
  border: 2px solid ${globalTokens.Button.default.value};

  &:hover {
    background-color: ${globalTokens.Button.default.value};
    color: #ffffff;
    border: 2px solid ${globalTokens.Button.default.value};
  }

  @media (max-width: 768px) {
  }
`;

// ---------------Medium
export const MediumButtonTypeA = styled.button`
  width: 100%;
  height: 100%;
  background-color: ${globalTokens.Button.default.value};
  color: #ffffff;
  font-size: 20px;
  cursor: pointer;
  border-radius: 20px;
  border: none;

  &:hover {
    background-color: #ffffff;
    color: ${globalTokens.Button.default.value};
    border: 2px solid ${globalTokens.Button.default.value};
  }

  @media (max-width: 768px) {
  }
`;
export const MediumButtonTypeB = styled.button`
  width: 100%;
  height: 100%;
  background-color: ${globalTokens.Button.default.value};
  color: #ffffff;
  font-size: 21px;
  cursor: pointer;
  border-radius: 90px;
  border: none;

  &:hover {
    background-color: #ffffff;
    color: ${globalTokens.Button.default.value};
    border: 2px solid ${globalTokens.Button.default.value};
  }

  @media (max-width: 768px) {
  }
`;
export const MediumButtonTypeC = styled.button`
  width: 100%;
  height: 100%;
  background-color: #ffffff;
  color: ${globalTokens.Button.default.value};
  font-size: 21px;
  cursor: pointer;
  border-radius: 20px;
  border: 2px solid ${globalTokens.Button.default.value};

  &:hover {
    background-color: ${globalTokens.Button.default.value};
    color: #ffffff;
    border: 2px solid ${globalTokens.Button.default.value};
  }

  @media (max-width: 768px) {
  }
`;
export const MediumButtonTypeD = styled.button`
  width: 100%;
  height: 100%;
  background-color: #ffffff;
  color: ${globalTokens.Button.default.value};
  font-size: 21px;
  cursor: pointer;
  border-radius: 90px;
  border: 2px solid ${globalTokens.Button.default.value};

  &:hover {
    background-color: ${globalTokens.Button.default.value};
    color: #ffffff;
    border: 2px solid ${globalTokens.Button.default.value};
  }

  @media (max-width: 768px) {
  }
`;

// ---------------Small
export const SmallButtonTypeA = styled.button`
  width: 100%;
  height: 100%;
  background-color: ${globalTokens.Button.default.value};
  color: #ffffff;
  font-size: 16px;
  cursor: pointer;
  border-radius: 20px;
  border: none;

  &:hover {
    background-color: #ffffff;
    color: ${globalTokens.Button.default.value};
    border: 2px solid ${globalTokens.Button.default.value};
  }

  @media (max-width: 768px) {
    font-size: 14px;
  }
`;
export const SmallButtonTypeB = styled.button`
  width: 100%;
  height: 100%;
  background-color: ${globalTokens.Button.default.value};
  color: #ffffff;
  font-size: 16px;
  cursor: pointer;
  border-radius: 90px;
  border: none;

  &:hover {
    background-color: #ffffff;
    color: ${globalTokens.Button.default.value};
    border: 2px solid ${globalTokens.Button.default.value};
  }

  @media (max-width: 768px) {
    font-size: 14px;
  }
`;
export const SmallButtonTypeC = styled.button`
  width: 100%;
  height: 100%;
  background-color: #ffffff;
  color: ${globalTokens.Button.default.value};
  font-size: 16px;
  cursor: pointer;
  border-radius: 20px;
  border: 2px solid ${globalTokens.Button.default.value};

  &:hover {
    background-color: ${globalTokens.Button.default.value};
    color: #ffffff;
    border: 2px solid ${globalTokens.Button.default.value};
  }

  @media (max-width: 768px) {
    font-size: 14px;
  }
`;
export const SmallButtonTypeD = styled.button`
  width: 100%;
  height: 100%;
  background-color: #ffffff;
  color: ${globalTokens.Button.default.value};
  font-size: 16px;
  cursor: pointer;
  border-radius: 90px;
  border: 2px solid ${globalTokens.Button.default.value};

  &:hover {
    background-color: ${globalTokens.Button.default.value};
    color: #ffffff;
    border: 2px solid ${globalTokens.Button.default.value};
  }

  @media (max-width: 768px) {
    font-size: 14px;
  }
`;

// ---------------PageNation
export const PageButtonType = styled.button`
  width: 100%;
  height: 100%;
  background-color: #ffffff;
  color: ${globalTokens.Button.default.value};
  font-size: 14px;
  cursor: pointer;
  border-radius: 50%;
  border: 2px solid ${globalTokens.Button.default.value};

  &:hover {
    background-color: ${globalTokens.Button.default.value};
    color: #ffffff;
    border: 2px solid ${globalTokens.Button.default.value};
  }

  @media (max-width: 768px) {
    font-size: 12px;
  }
`;
